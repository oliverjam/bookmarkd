import React from 'react';
// import { shallow, mount, render } from 'enzyme';
import BookRows from './BookRows';

describe('Test suite for bookrows component', () => {
  it('should return bookrows component', () => {
    expect(true).toBe(true);
    // const bookRows = shallow(<BookRows />);
    // expect(
    //   bookRows.instance().getGenres(['fiction', 'nonfiction', 'fiction'])
    // ).toBe(['fiction', 'nonfiction']);
    // expect(bookRows.dive()).contains(<div className="bookRows" />)).toBe( true);
  });
});
